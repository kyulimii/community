# Gradle/IDE 캐시 - 이미지 빌드는 컨테이너 내부에서 새로 수행하므로 불필요
.gradle
.git
.idea
*.iml

# 호스트에서 만들어진 산출물 - 컨테이너 안에서 다시 빌드하므로 build context에 넣을 필요 없음
build

*.md
*.log

# 비밀 값이 담긴 로컬 환경설정 - 이미지에 절대 포함되면 안 됨
.env
.env.*

# Dockerfile 자체와 무관한 파일 - build context 전송 용량 절감
Dockerfile
.dockerignore